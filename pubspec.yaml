name: wallet_app
description: "A simple wallet quiz app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  in_app_purchase: ^3.2.0
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
  
